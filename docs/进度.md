# SelfCoach 项目进度台账

- 维护人：架构窗口（负责记录进度、合并审查）
- 最后更新：2026-09-09

> 本文档是项目的「实时进度台账」。架构窗口在每次功能推进 / 验证 / 合并后更新。
> 与《需求文档》《技术文档》《开发计划》《团队分工》配合阅读。

---

## 1. 当前状态（一句话）

🚧 **Phase 0 进行中**：基础设施（建表 / 连库）与 F-01、F-02 已完成并入 main；F-03 知识问答(RAG) 的「入库管线」已完成并实测，正推进「混合检索」。

---

## 2. Phase 0 功能进度

| 编号 | 功能 | 分支 | 状态 | 验证 / 合并 |
|---|---|---|---|---|
| 基础设施 | 云服务器建表（4 表 + pgvector HNSW） | — | ✅ 完成 | 服务器实执行，\dt 4 表 |
| 基础设施 | 后端接入数据库（R2DBC + Docker 化） | `main` | ✅ 完成 | 日志连库 OK |
| F-01 | 训练记录 CRUD | `feature/training-log` | ✅ 完成 | 云服务器 curl 四操作通过，已合 main |
| F-02 | 身体指标记录（含同天覆盖合并） | `feature/body-metric` | ✅ 完成 | 云服务器 curl 三接口通过，已合 main |
| F-03 | 知识库问答(RAG)：入库管线 | `feature/rag` | ✅ 完成 | 分块+Embedding+写 doc_chunk 实测维度1024 |
| F-03 | 知识库问答(RAG)：混合检索 | `feature/rag` | 🚧 待后端窗口 | 开工卡片已出 |
| F-03 | 知识库问答(RAG)：问答生成带引用 | `feature/rag` | ⬜ | 待检索完成后 |
| F-05 | 对话式交互（前端） | `feature/chat-ui` | ⬜ | 待前端窗口 |
| F-04 | 训练计划生成 / 其余 | — | ⬜ | Phase 1 |

> 图例：⬜ 未开始　🚧 进行中　✅ 已验收

---

## 3. 详细开发记录

### 基建
- ✅ **建表**：`db/schema.sql` 建 `user_profile / training_log / body_metric / doc_chunk`，含 pgvector HNSW 向量索引；云服务器 docker 一键 `scripts/init-db.sh` 执行。
- ✅ **接入**：后端加 spring-data-r2dbc + r2dbc-postgresql，连接走环境变量；Docker 多阶段构建（阿里云 Maven 镜像解决 central 不通）；端口 8081（避开占用的 8080）。

### F-01 训练日志 CRUD
- 四层：`TrainingLog` 实体 / Repository / Service / Controller。
- **验收**：POST/GET/PUT/DELETE 全跑通。已合并 main。

### F-02 身体指标
- 三接口：POST（同天字段级覆盖合并）/ 范围查询(升序) / latest。
- **验收**：覆盖合并实测通过（只传体重、腰围保留）。已合并 main。

### F-03 RAG · 入库管线
- `DocumentChunker`（中文分块，按段落边界 + maxChars 兜底）
- `EmbeddingClient`（WebClient 手写调阿里云百炼 `/embeddings`，1024 维）
- `IngestService`（分块→embed→原生 SQL `:embedding::vector` 写入 doc_chunk，绕开 r2dbc 不支持 vector 的问题）
- `IngestController`（POST /api/ingest）
- **验收**：POST /api/ingest 写入 1 块，`vector_dims(embedding)=1024`。

### 待办（进行中）
- 第 6 步 **混合检索**（后端窗口认领中）：Lucene BM25 + 向量检索 + RRF 融合 + 重排 → GET /api/search。
- 第 7 步 **问答生成带引用**（后端窗口）：LLM `qwen3.8-flash` + Top-K 拼提示词 + 来源引用。
- 第 8 步 **前端对话界面**（前端窗口认领 `feature/chat-ui`）。

---

## 4. 团队协作与分支

- 多窗口按角色协作：架构/主仓 + 后端 + 前端 + 测试 + 代码审查。详见 `docs/团队分工.md`。
- 分支策略：Feature Branch，功能独立分支，架构用 `--no-ff` 合并回 main。详见 `docs/git工作流.md`。
- **架构职责**：定义人接口契约、出开工卡片、审查与联测、合并回 main、**维护本进度台账**；不直接写业务代码（RAG 第 6/7 步由后端窗口实现）。

### 分支当前指向
| 分支 | 说明 |
|---|---|
| `main` | 稳定，含 F-01、F-02（`4a00579`） |
| `feature/rag` | RAG 进行中：入库已完成，混合检索待做（`da5dd32`） |
| `feature/training-log`、`feature/body-metric` | 已合并 main（可删可留） |
| `feature/chat-ui` | 骨架，待前端 |

---

## 5. 技术备忘（踩过的坑 / 关键约束）

- **Maven central 国内服务器不通** → 用阿里云镜像 `maven-settings.xml`。
- **8080 被服务器另一服务占用** → SelfCoach 后端走 **8081**（docker-compose 已配）。
- **r2dbc-postgresql 默认不支持 vector 列** → 用 DatabaseClient 原生 SQL + `:embedding::vector` 写入。
- **Embedding 维度必须 = vector(1024)**：用 `qwen3.7-text-embedding-flash` 的 `dimensions:1024`，已实测匹配。
- 云服务器后端验证：`docker compose up -d --build backend`；数据库密码等敏感信息走 `.env`，不进库不进 git。

---

## 6. 下一步

1. 后端窗口按开工卡片实现**第 6 步混合检索**（feature/rag），完成 push 后交架构审查联测。
2. 架构审查通过后推进**第 7 步问答生成带引用**。
3. RAG 三环全部完成、联测后，将 `feature/rag` 合并回 `main`。
4. 随后启动**前端 F-05**（feature/chat-ui）。

> 协作约定不变：一次只推一个功能 → 讲清 → 验证 → 合并 → 更新本台账。